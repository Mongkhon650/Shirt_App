import 'package:flutter/material.dart';

// Ordered Products Component
class OrderedProductTile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'สินค้าที่ถูกสั่งซื้อ',
      ),
    );
  }
}
